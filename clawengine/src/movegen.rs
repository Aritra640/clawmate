use crate::{board::Board, chessmove::ChessMove};

pub struct MoveGenerator;

impl MoveGenerator {
    pub fn generate(board: &Board) -> Vec<ChessMove> {
        let mut moves = Vec::new();

        moves
    }
}
